---
# Copy to secrets.yml and fill in, or inject via AAP credentials / vault.
# Offline token: https://console.redhat.com/openshift/token
rh_offline_token: "eyJhbGciOiJSUzI1NiIs..."

# Optional override if pull secret is not at secrets/pull-secret.json
# ai_pull_secret_file: /path/to/pull-secret.json
