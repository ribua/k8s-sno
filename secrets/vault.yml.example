---
# Copy to vault.plain.yml, set values, run: ./scripts/seal-secrets.sh
# Offline token: https://console.redhat.com/openshift/token
rh_offline_token: "PASTE_OFFLINE_TOKEN_HERE"
# SSH public key embedded in discovery ISO (core@ access during install)
rh_ssh_public_key: "ssh-rsa AAAA... user@host"
